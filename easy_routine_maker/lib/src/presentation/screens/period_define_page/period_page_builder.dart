import 'package:flutter/material.dart';

class PeriodPageBuilder extends StatelessWidget {
  const PeriodPageBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}